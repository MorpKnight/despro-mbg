# Example environment variables for Despro MBG frontend
# Copy this file to `.env` and adjust values as needed.

# Public API base URL used by the app.
# If not set, the app falls back to `expo.extra.apiUrl` in app.json
# or to `http://10.0.2.2:8000/api/v1` for Android emulator.
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1

# Optional: override AsyncStorage server URL key at first launch.
# Usually you don't need to set this; the app will use
# EXPO_PUBLIC_API_URL or the value stored in AsyncStorage.
# SERVER_URL=
